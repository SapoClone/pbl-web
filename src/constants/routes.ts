export enum ROUTES {
  ROOT = "/",
  SCREEN = "screen",
  APP = "app",
  ROOM = "room",
  SETTING = "setting"
}

export enum ROOM_ROUTES {
  LIGHT = "light",
  WINDOW = "window",
  RELAY = "relay",
  TEMPERATURE = "temperature"
}

export enum APP_ROUTES {
  POSITION = "position",
  POSITION_ID = "$positionId"
}
